spring:
  resources:
    static-locations: classpath:/static
  mvc:
    static-path-pattern: /static/**
thread.pool:
  instances:
    - name: threadpool1
      corePoolSize: 2
      maxPoolSize: 10
    - name: threadpool2
      corePoolSize: 2
      maxPoolSize: 10

